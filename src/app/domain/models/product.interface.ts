export interface ProductInterface {
  productId?: string,
  code?: string,
  name?: string,
  description?: string,
  stock?: number,
  image?: string,
  price?: number,
  categoryId?: string,
  iva?: boolean,
}
